#include <stdio.h>


int main(){
    int r, g , b , m;

    scanf("%d %d %d",& r, &g, &b);
    scanf("%d",&m);

    r = (r & 254) | ((m>>2)&1);
    g = (g>>254 ) | ((m>>1)&1);
    b = (b & 254) | (m&1);

    printf("%d %d %d", r,g,b);
    return 0;
}
